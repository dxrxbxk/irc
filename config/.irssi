servers = (
  {
    address = "localhost";
    port = "8080";
    use_ssl = "no";
    ssl_verify = "no";
    autoconnect = "yes";
  }
);

settings = {
  "fe-common/core" = { autolog = "yes"; };
  core = {
    real_name = "real_richtofen";
    user_name = "user_richtofen";
    nick = "poutine";
  };
  "fe-text" = { actlist_sort = "refnum"; };
};

# Si vous utilisez un script pour l'authentification, assurez-vous de le charger ici.
# Cependant, je recommande fortement d'utiliser une méthode plus sécurisée pour stocker le mot de passe.

logs = { };
